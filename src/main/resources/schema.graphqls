type Query {
    bookById(id: ID): Book
    books: [Book]
}

type Book {
    id: ID
    description: String
    title: String
    authorId: Int
    author: Author!
}

type Author {
    id: ID
    name: String
    age: Int
}