type Query {
    bookById(id: ID): Book
    books: [Book]
}

type Book {
    id: ID
    description: String
    title: String
    authorId: ID
    author: Author!
}

type Author {
    id: ID
    name: String
    age: Int
}